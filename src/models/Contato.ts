class Contato {
  titulo: string
  email: string
  numero: string
  id: number

  constructor(titulo: string, email: string, numero: string, id: number) {
    this.titulo = titulo
    this.email = email
    this.numero = numero
    this.id = id
  }
}

export default Contato
